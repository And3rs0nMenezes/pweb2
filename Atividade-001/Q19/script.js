// Crie um programa que gere uma lista de números pares de 1 a 50 usando um loop for.
// Em seguida, exiba essa lista no console.
for (i = 0; i <= 50; i++) {
    if (i % 2 == 0)
        console.log(i)
}