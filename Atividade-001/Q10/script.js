// Crie uma variável chamada "frutas" que contenha um array com três nomes de frutas.
// Use um loop for para exibir cada fruta no console.
const frutas = ['Banana', 'Pera', 'Abacaxi']
frutas.forEach(nome => {
    console.log(nome)
});